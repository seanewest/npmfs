{
  "name": "npmfs",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "./install.sh",
    "uninstall": "umount /usr/local/npmfs; rm -f ~/Library/LaunchAgents/npmfs-service.plist; rm -f /etc/paths.d/npmfs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sean West",
  "license": "MIT",
  "dependencies": {
    "fuse4js": "^0.1.9",
    "mkdirp": "^0.5.0",
    "npm": "^1.4.21",
    "rimraf": "^2.2.8"
  }
}
